# Production origin used for canonicals, Open Graph, JSON-LD, robots.txt and sitemap.xml.
# HTTPS, no trailing slash. Set this in Cloudflare Pages → Settings → Environment variables.
VITE_PUBLIC_SITE_URL=https://your-project.pages.dev
